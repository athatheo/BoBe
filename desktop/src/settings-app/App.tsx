/**
 * Settings app root component
 *
 * Renders the Bobe Tuning settings window.
 */

import { SettingsWindow } from '@/features/settings/SettingsWindow'

export default function App() {
  return <SettingsWindow />
}
